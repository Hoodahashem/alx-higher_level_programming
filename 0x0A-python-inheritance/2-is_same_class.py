#!/usr/bin/python3

"""let's start creating a new function!"""


def is_same_class(obj, a_class):
    """docstring for is_same_class"""
    if type(obj) == a_class:
        return True
    else:
        return False
